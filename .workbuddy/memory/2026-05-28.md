# 2026-05-28

## 添加计算列功能实现
- 新增 `Services/FormulaParser.cs`：公式解析与RC格式求值引擎
  - 支持 `${列名}` 格式的列引用
  - 将用户公式转换为Excel RC格式 `$[R[x]C[y]]`
  - 递归下降解析器支持 `+` `-` `*` `/` `()` 和浮点数
  - RC引用求值：支持 `$[RC[-n]]`、`$[R[-1]C]` 等相对引用
- 新增 `AddComputedColumnDialog.xaml/.cs`：添加计算列对话框
  - 列名输入（验证不重复）、公式输入（实时语法验证）
- 修改 `Models/FlightData.cs`：添加 `ColumnOrder`、`ComputedColumnFormulas`、`ComputedColumnRcFormulas`
- 修改 `Services/CsvFlightImportService.cs`：
  - 记录列顺序到 `ColumnOrder`
  - 检测RC公式列（值匹配 `$[...]` 格式）
  - 计算列实时求值存入 `Parameters`
- 修改 `MainWindow.xaml`：右键菜单添加"添加计算列"
- 修改 `MainWindow.xaml.cs`：添加 `AddComputedColumn_Click` 事件处理
- 修改 `ViewModels/MainViewModel.cs`：
  - 添加 `AddComputedColumn` 方法
  - 添加 `AppendComputedColumnToCsv` 方法（CSV追加列）
  - `ColumnNode` 添加 `IsComputed` 属性
## 后续修复
- CSV乱码修复：`AppendComputedColumnToCsv` 从 `ReadAllLines`+`WriteAllLines`（会重新编码）改为字节级追加（`ReadAllBytes`→按`\n`切分→每行追加新列字节→`WriteAllBytes`），完全保留原文件编码
- FormulaParser修复：`EvaluateAtRow` 中RC引用返回NaN时不再进入Tokenizer（会崩溃），直接返回NaN；新增 `InterpolationContext` 支持越界插值
- 计算列不显示强调点：`CurveItem.IsComputed` + `Refresh` 中 `MarkerSize=0`
- CSV编码检测：UTF-8 BOM 改为 `new UTF8Encoding(true)` 保留BOM写回
- NuGet构建问题：Git Bash 缺少 `ProgramData` 环境变量导致 NuGet restore 失败（预存在的环境问题，需用 VS 构建）
